Machine-readable gate reports land here. Every file beside this one is
regenerated on each `npm run ci` and is gitignored; this placeholder is the one
tracked file, so a fresh clone has the directory the runner writes into.
