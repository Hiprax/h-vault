node_modules
dist
dist-sandbox
build
coverage
.cache
test-results
playwright-report
dev-dist
package-lock.json
logs
packages/shared/src/generated
*.tmp

# Byte-exact test fixtures. These are DATA, not source: the export goldens are
# the exact bytes another password manager parses (reformatting the JSON golden
# would rewrite the artifact the assertion compares against), and the fuzz
# corpus contains a deliberately truncated JSON document that Prettier cannot
# parse at all. See the matching -text rules in .gitattributes.
packages/client/tests/export/goldens
packages/client/tests/fuzz/corpus
packages/client/tests/fuzz/format-corpus

# The end-to-end document fixtures, for the same reason: `broken.json` is
# deliberately unparseable (an elided array element, which the repair engine is
# asserted to REFUSE, naming the line) and `ugly.json` is deliberately
# unformatted (single quotes and trailing commas, which the same engine is
# asserted to repair and reformat). Prettier would fail on the first and rewrite
# the second into the file the assertion says it must produce.
e2e/fixtures

# Mutation testing scratch: Stryker sandbox copies of the whole checkout.
.stryker-tmp
